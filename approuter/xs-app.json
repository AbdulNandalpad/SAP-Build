{
  "authenticationMethod": "route",
  "sessionTimeout": 60,
  "errorPage": [
    {"status": [401, 403], "file": "/error.html"}
  ],
  "logout": {
    "logoutEndpoint": "/logout",
    "logoutPage": "/"
  },
  "routes": [
    {
      "source": "^/odata/(.*)",
      "destination": "tss-backend",
      "authenticationType": "xsuaa",
      "csrfProtection": false
    },
    {
      "source": "^/user-api/(.*)",
      "service": "sap-approuter-userapi",
      "authenticationType": "xsuaa"
    },
    {
      "source": "^/(.*)",
      "localDir": "ui",
      "authenticationType": "xsuaa"
    }
  ]
}
